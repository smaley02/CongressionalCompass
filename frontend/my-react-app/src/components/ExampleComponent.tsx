const ExampleComponent: React.FC = () => {
    return (
        <div>
            <h1>Hello from ExampleComponent!</h1>
            <p>This is an example component in a React application.</p>
        </div>
    );
};

export default ExampleComponent;